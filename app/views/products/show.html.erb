<h1><%= @product.name %></h1>

<%= link_to "Edit", edit_product_path(@product) %>
<%= link_to "Back", products_path %>
<%= button_to "Delete", @product, method: :delete, data: { turbo_confirm: "Are you sure you wish to delete #{@product.name}?"} %>